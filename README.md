# 21F3002319_MLOPS_WEEKLY_ASSIGNMENT Week 2
.dvc folder - contains config file for .dvc.

data folder - contains lightweight dvc pointers for iris.csv dataset.

prepare_data.ipynb - splits the dataset into train and test datasets.

train.ipynb - trains decision tree model on train dataset.

inference.ipynb - evaluates the model on test dataset.

artifacts.dvc - contains lightweight dvc pointers for trained model.
